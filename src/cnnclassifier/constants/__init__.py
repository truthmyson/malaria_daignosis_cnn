# this will contain all variables which wont change through out the project
# example: config.yaml, params.yaml etc

from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")